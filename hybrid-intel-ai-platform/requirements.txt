# Core
streamlit>=1.32
pandas>=2.0
plotly>=5.18

# Force-directed link analysis (Tab 2)
streamlit-agraph>=0.0.45

# Optional LLM providers (install whichever you want active)
# The pipeline auto-detects which key is set; if neither SDK is
# installed or no key is exported, it falls back to heuristics.
anthropic>=0.40
# openai>=1.40
